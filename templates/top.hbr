<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{URL}}style.css">
    <link rel="stylesheet" href="{{URL}}invstyle.css">
    <script src="{{URL}}app.js" defer></script>

    <title>{{pageTitle}}</title>
</head>

<body>

    <header>
   {{#if message}}
    <div ; style="text-align: center;" class="container mt-5" data-msg>
        <div class="row">
            <div class="col-4"></div>
            <div class="col-4" >
                <div style="font-size:xx-large"  class="alert alert-{{message.type}}" role="alert">
                   {{message.text}}
                </div>
            </div>
            <div class="col-4"></div>
        </div>
    </div>
    {{/if}}
    

       


    </header>